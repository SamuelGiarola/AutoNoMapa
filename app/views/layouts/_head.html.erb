<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">

     <%= link_to image_tag("/imgs/brand.png"), root_path, :class =>"brand" %>
     <div class="nav-collapse">

        <ul class="menu-nav pull-right">
          <% if freelance_signed_in? %>
            <li>
              <%= link_to('Logout', destroy_freelance_session_path, :method => :delete) %>
            </li>
            <% else %>
          <li class="menu-nav-sentence">É Autonômo?</li>
            <li>
              <%= link_to('Divulgue seus serviços', new_freelance_registration_path, :class => "btn btn-info")  %>
            </li>
            <li class="menu-nav-sentence">ou</li>
            <li>
              <%=link_to(new_freelance_session_path, :class =>"btn") do%>
                <i class="icon-user"></i>Acesse seu perfil
              <% end %>
            </li>
          <%end%>
         </ul>


      </div>

    </div>
  </div>
</div>
